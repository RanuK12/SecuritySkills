rules:
  - id: hardcoded-api-key
    patterns:
      - pattern: |
          $API_KEY = "..."
      - pattern: |
          $API_KEY = '...'
    message: Detected hardcoded API key (consider using environment variable or secret manager)
    severity: ERROR
    languages: [Python]
